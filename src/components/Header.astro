---
// Header.astro
---

<header class="relative h-screen flex items-center justify-center">
  {/* Imagen de fondo con overlay */}
  <div class="absolute inset-0 z-0">
    <img
      src="/foto-boda.jpg"
      alt="Foto boda"
      class="w-full h-full object-cover object-center"
      loading="eager"
      decoding="async"
    />
    <div class="absolute inset-0 bg-linear-to-b from-gray-900/40 via-gray-900/20 to-gray-900/40"></div>
  </div>

  {/* Contenido del header */}
  <div class="text-center text-white relative z-10 px-4 space-y-8 md:space-y-12">
    <div class="space-y-4 md:space-y-6">
      <h1 class="text-5xl sm:text-6xl md:text-7xl lg:text-8xl font-serif tracking-wide animate-fade-in">
        Paula & Ronaldo
      </h1>
      <p class="text-xl md:text-2xl lg:text-3xl font-light opacity-90">
        ¡Nos Casamos!
      </p>
    </div>

    {/* Fecha destacada */}
    <div class="text-lg md:text-2xl lg:text-3xl font-medium bg-white/10 backdrop-blur-md rounded-xl p-4 md:p-6 border border-white/30 w-fit mx-auto hover:bg-white/15 transition-colors">
      10 de Octubre de 2026
    </div>

    {/* Mensaje especial */}
    <div class="max-w-2xl mx-auto px-4">
      <p class="text-lg md:text-xl lg:text-2xl font-light leading-relaxed opacity-95">
        Estamos muy felices de compartir este momento especial con ustedes.
        <br />
        <span class="text-rose-300">Acompáñanos en la celebración de nuestro amor</span>
      </p>
    </div>
  </div>
</header>

<style>
  @keyframes fade-in {
    from {
      opacity: 0;
      transform: translateY(1rem);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .animate-fade-in {
    animation: fade-in 1.2s cubic-bezier(0.4, 0, 0.2, 1) forwards;
  }
</style>